<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>🌀 Chuzy-OS • AI in Progress</title>
    <link rel="icon" href="favicon.ico" type="image/x-icon">
    <link rel="apple-touch-icon" href="favicon.png">
    <style>
        body {
            margin: 0;
            padding: 0;
            background-color: #f5f5f5;
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            text-align: center;
            color: #333;
        }
        .logo {
            width: 25vw;
            max-width: 120px;
            height: auto;
            aspect-ratio: 1 / 1;
            background-color: #1a73e8;
            border-radius: 50%;
            display: flex;
            justify-content: center;
            align-items: center;
            color: white;
            font-size: 10vw;
            max-font-size: 48px;
            font-weight: bold;
            margin-bottom: 20px;
            animation: pulse 2s infinite ease-in-out;
            transition: transform 0.3s ease-in-out, box-shadow 0.3s ease-in-out;
        }
        .logo:hover {
            transform: scale(1.1) rotate(10deg);
            box-shadow: 0 6px 15px rgba(0,0,0,0.2);
        }
        @keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.05); }
            100% { transform: scale(1); }
        }
        h1 {
            font-size: 8vw;
            max-font-size: 48px;
            font-weight: bold;
            margin-bottom: 15px;
        }
        p.tagline {
            font-size: 4vw;
            max-font-size: 20px;
            color: #555;
            margin-bottom: 25px;
            transition: opacity 0.4s ease-in-out;
        }
        p.contact {
            font-size: 4vw;
            max-font-size: 18px;
            margin-bottom: 25px;
        }
        p.contact a {
            color: #1a73e8;
            text-decoration: none;
            animation: emailPulse 3s infinite alternate;
        }
        p.contact a:hover {
            text-decoration: underline;
        }
        @keyframes emailPulse {
            0% { color: #1a73e8; }
            50% { color: #34a853; }
            100% { color: #1a73e8; }
        }
        p.dev-contact {
            font-size: 3vw;
            max-font-size: 14px;
            color: #888;
            margin-bottom: 5px;
        }
        p.version {
            font-size: 3vw;
            max-font-size: 12px;
            color: #888;
            margin-bottom: 10px;
        }
        p.github-badge img {
            height: 20px;
            margin-bottom: 10px;
        }
        .progress-container {
            width: 60%;
            max-width: 400px;
            height: 6px;
            background-color: #ddd;
            border-radius: 3px;
            margin: 20px auto 10px auto;
            overflow: hidden;
        }
        .progress-bar {
            width: 0%;
            height: 100%;
            background-color: #1a73e8;
            border-radius: 3px;
            animation: loadProgress 4s ease-in-out infinite;
        }
        @keyframes loadProgress {
            0% { width: 0%; }
            50% { width: 80%; }
            100% { width: 0%; }
        }
        footer {
            font-size: 3vw;
            max-font-size: 12px;
            color: #aaa;
        }
    </style>
</head>
<body>
    <div class="logo">C</div>
    <h1>Chuzy-OS</h1>
    <p class="tagline" id="tagline">An experimental AI system currently under development</p>
    <p class="contact">Contact: <a href="mailto:chuzyos.ai@gmail.com">chuzyos.ai@gmail.com</a></p>
    <p class="dev-contact">
        Developed by <a href="https://github.com/chuzyos" target="_blank">Chuzy-OS Team</a>
    </p>
    <p class="version">Version 0.1 • Build 2025-12-28</p>
    <p class="github-badge">
        <a href="https://github.com/chuzyos" target="_blank">
            <img src="https://img.shields.io/github/stars/chuzyos?style=social" alt="GitHub stars">
        </a>
    </p>
    <div class="progress-container">
        <div class="progress-bar"></div>
    </div>
    <footer>© 2025 Chuzy-OS. All rights reserved. More updates coming soon.</footer>

    <script>
        const taglines = [
            "An experimental AI system currently under development",
            "Exploring the frontiers of intelligent systems",
            "Powered by curiosity and code",
            "Building the future, one algorithm at a time"
        ];
        
        let index = 0;
        const taglineElement = document.getElementById("tagline");

        setInterval(() => {
            index = (index + 1) % taglines.length;
            taglineElement.style.opacity = 0;
            setTimeout(() => {
                taglineElement.textContent = taglines[index];
                taglineElement.style.opacity = 1;
            }, 400);
        }, 4000);
    </script>
</body>
</html>